import React from 'react';

import { TabLineStyled } from './styled';

/**
 * @export
 * @component
 * @name TabLine
 *
 * @description
 * Responsável por conter a linha embaixo da tab
 */
export const TabLine = (): JSX.Element => {
  return <TabLineStyled />;
};
